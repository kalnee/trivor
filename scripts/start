#!/bin/bash

SERVICE=$1

if [ "$SERVICE" == "trivor-insights" ] || [ -z "$SERVICE" ]; then
    echo "Building trivor-insights"
    cd trivor-insights && ./gradlew clean build
    wait
fi

echo "Running docker-compose"
docker-compose stop $SERVICE
docker-compose build $SERVICE
docker-compose up $SERVICE