server:
  port: 8080
app:
  env: dev
spring:
  application:
    name: trivor-engine
  data:
    mongodb:
      host: localhost
      port: 27017
cloud:
  aws:
    region:
      static: us-east-1
    credentials:
      instanceProfile: false
sqs:
  queue:
    insights: trivor-insights-${app.env}
